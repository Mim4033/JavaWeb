
version: 0.2
phases:
  install:
    runtime-versions:
      java: corretto17
  build:
    commands:
      - echo Build completed on `date`
      - mvn package
  post_build:
    commands:
      - echo Build completed on `data`
artifacts:
  files:
    - targetJavaWeb-0.0.1-SNAPSHOT.jar
  discard-paths: yes

